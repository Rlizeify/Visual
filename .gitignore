node_modules/
dist/
dist-electron/
*.local
.DS_Store
Thumbs.db
*.db
*.db-journal
.env
apps/desktop/node_modules/
apps/desktop/dist/
apps/desktop/dist-electron/

# Keep vendor tool builds (bundled single-file apps)
!vendor/*/dist/

# Stray Supabase CLI cache if invoked from the repo root by accident
# (the canonical location is web/supabase/, ignored by web/supabase/.gitignore)
/supabase/.temp/
/supabase/.branches/

# Vercel project config (contains project IDs)
.vercel/

# Legacy nested clone of this same repo (session-17 era). Archived locally,
# never tracked. Removing it as a gitlink fixed Vercel submodule-fetch failures.
/Visual/
