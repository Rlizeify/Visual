{
  "name": "visual",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix apps/desktop",
    "dev": "npm run dev --prefix apps/desktop",
    "build": "npm run build --prefix apps/desktop"
  }
}
