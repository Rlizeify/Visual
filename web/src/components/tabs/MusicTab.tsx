// MusicTab is intentionally empty - the visualizer renders at the root level
// and stays mounted. This component exists only as a route target.
// When on the M tab, no fog overlay is shown and the viz is fullscreen.

export default function MusicTab() {
  return null
}
