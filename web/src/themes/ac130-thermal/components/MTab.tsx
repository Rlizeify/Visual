// MTab is intentionally empty for AC-130 Thermal — the visualizer
// is mounted at App root and the Decorations layer frames it with
// HUD chrome at low opacity (showOverlays: false on /m).
export default function AC130ThermalMTab() {
  return null
}
