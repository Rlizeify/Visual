// MTab is intentionally empty for Asian Vibrant. The visualizer is
// mounted at App root; the theme chrome (NavBar, WaveformBar,
// PlaybackControls, GearMenu) wraps around it.
export default function AsianVibrantMTab() {
  return null
}
