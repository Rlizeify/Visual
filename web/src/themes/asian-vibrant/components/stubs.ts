// All component surfaces for the Asian Vibrant theme return null — the
// theme's shell takes over the entire viewport while it's the active
// theme, so the per-surface components are never rendered.
const NullStub = () => null
export default NullStub
