import type { ReactNode } from 'react'

/**
 * Asian Vibrant shell.
 *
 * Pass-through. Like Frutiger Aero, the entire presentation is
 * delivered through DashboardShell and per-surface components;
 * the shell itself just hosts the route tree.
 *
 * The previous stub overlay (full-screen "coming soon" splash) is
 * archived at web/src/archive/asian-vibrant-stub/.
 */
export default function AsianVibrantShell({ children }: { children: ReactNode }) {
  return <>{children}</>
}
