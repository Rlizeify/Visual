// MTab is intentionally empty for Frutiger Aero — the visualizer is
// mounted at App root (so it can persist across route changes and stay
// behind the H/E/U fog overlay). A sibling theme that wants its own
// M-tab presentation can replace this stub with a real component.
export default function FrutigerAeroMTab() {
  return null
}
